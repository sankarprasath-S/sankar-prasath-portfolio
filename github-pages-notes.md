# GitHub Pages deployment note

GitHub Pages is configured to publish through the repository workflow. The initial workflow deployment succeeded, but the project-site base path rendered the application fallback route. The routing configuration now matches the Vite-provided project base path (`/sankar-prasath-portfolio/`) directly, and the next deployment will verify the public homepage.
